import { Section } from '../styles/Section';
import SellForm from '../components/PostItemForm';

const Sell = () => {
    return (
        <Section>
            <SellForm />
        </Section>
    );
};

export default Sell;
