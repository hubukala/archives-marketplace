import { Section } from "../styles/Section";
import LoginPopup from "../components/LoginPopup";

const Login = () => {
  return (
    <Section>
      <LoginPopup />
    </Section>
  );
};

export default Login;