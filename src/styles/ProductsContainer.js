import styled from 'styled-components';

const ProductsContainer = styled.div`
    width: 100%;
    height: auto;
    display: flex;
    flex-direction: row;
    flex-flow: wrap;
`;

export { ProductsContainer };
